# Draft one target section

Draft the requested target section in detailed Markdown. Preserve the target heading exactly in
the structured fields, but return only content beneath that heading in `content_markdown`.

Use all mapped supporting source blocks and retain their operational detail. Preserve the order of
sequential actions and use Markdown numbered lists for steps. Include the template's fixed text
verbatim before newly drafted content. Satisfy each supported requirement explicitly.

For partially supported, missing, conflicting, or unclear information, include the applicable
pre-existing gap callout exactly once and return its gap ID. Do not fill gaps with generic procedure
language. Cite only source block IDs that actually support populated content.
